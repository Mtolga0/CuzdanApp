import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cuzdan.app',
  appName: 'CüzdanApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
